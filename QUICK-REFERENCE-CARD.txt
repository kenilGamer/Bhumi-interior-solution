═══════════════════════════════════════════════════════════════════════
  BHUMI INTERIOR SOLUTION - MIGRATION QUICK REFERENCE CARD
═══════════════════════════════════════════════════════════════════════

📖 START HERE: README-MIGRATION.md

═══════════════════════════════════════════════════════════════════════
  FASTEST MIGRATION (5 COMMANDS) ⚡
═══════════════════════════════════════════════════════════════════════

ON CURRENT SERVER:
  cd /var/www/bhumi-interior
  sudo ./scripts/complete-backup.sh

ON LOCAL MACHINE:
  scp -r root@CURRENT_IP:/var/backups/bhumi-interior/migration-* ./backup/
  scp -r ./backup/* root@NEW_IP:/tmp/

ON NEW SERVER:
  curl -fsSL https://get.docker.com | sh
  mkdir -p /var/www/bhumi-interior && cd /var/www/bhumi-interior
  # Upload restore script, then:
  ./restore-from-backup.sh /tmp/migration-YYYYMMDD_HHMMSS/

UPDATE DNS:
  A @ NEW_IP
  A www NEW_IP  
  A api NEW_IP

═══════════════════════════════════════════════════════════════════════
  DOCUMENTATION GUIDE
═══════════════════════════════════════════════════════════════════════

README-MIGRATION.md         → Start here (5 min read)
MIGRATION-QUICK-START.md    → Fast track (1-2 hours)
MIGRATION-GUIDE.md          → Complete guide (2-3 hours)
MIGRATION-INDEX.md          → Navigation & reference
MIGRATION-TOOLKIT.md        → Visual maps & diagrams
DEPLOYMENT.md               → Production reference

═══════════════════════════════════════════════════════════════════════
  KEY SCRIPTS
═══════════════════════════════════════════════════════════════════════

scripts/complete-backup.sh        Backup everything
scripts/restore-from-backup.sh    Restore on new server
scripts/mongodb-backup.sh         Database backup only
scripts/mongodb-restore.sh        Database restore only
scripts/setup-ssl.sh              Setup HTTPS/SSL
scripts/setup-cron-backup.sh      Automated backups

═══════════════════════════════════════════════════════════════════════
  ESSENTIAL COMMANDS
═══════════════════════════════════════════════════════════════════════

CHECK STATUS:
  docker ps
  docker-compose -f docker-compose.atlas.yml ps

VIEW LOGS:
  docker-compose -f docker-compose.atlas.yml logs -f
  docker-compose -f docker-compose.atlas.yml logs backend

RESTART SERVICES:
  docker-compose -f docker-compose.atlas.yml restart
  docker-compose -f docker-compose.atlas.yml restart backend

START/STOP:
  docker-compose -f docker-compose.atlas.yml up -d
  docker-compose -f docker-compose.atlas.yml down

TEST BACKEND:
  curl http://localhost:5000/

TEST DATABASE:
  docker exec bhumi-interior-backend node -e \
    "const m=require('mongoose'); \
     m.connect(process.env.MONGO_URI) \
     .then(()=>console.log('✅')) \
     .catch(e=>console.log('❌',e.message))"

═══════════════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════

CONTAINERS WON'T START:
  docker-compose -f docker-compose.atlas.yml logs
  docker-compose -f docker-compose.atlas.yml down
  docker-compose -f docker-compose.atlas.yml up -d --build

DATABASE CONNECTION FAILS:
  Check MongoDB Atlas Network Access → Add NEW_SERVER_IP
  Verify MONGO_URI in .env.production

SSL FAILS:
  dig bhumiinteriorsolution.in  (verify DNS)
  ufw status                     (check firewall)
  Wait for DNS propagation (5-60 minutes)

FRONTEND BLANK:
  cd frontend && npm run build
  docker-compose -f docker-compose.atlas.yml restart nginx

═══════════════════════════════════════════════════════════════════════
  SERVER REQUIREMENTS
═══════════════════════════════════════════════════════════════════════

MINIMUM:               RECOMMENDED:
• 2 CPU cores          • 4 CPU cores
• 2GB RAM              • 4GB+ RAM  
• 20GB storage         • 40GB SSD storage
• Ubuntu 20.04+        • Ubuntu 22.04 LTS

REQUIRED PORTS:
• 22  (SSH)
• 80  (HTTP)
• 443 (HTTPS)

═══════════════════════════════════════════════════════════════════════
  PRE-MIGRATION CHECKLIST
═══════════════════════════════════════════════════════════════════════

□ Current server SSH access
□ New server created (2GB+ RAM)
□ Domain registrar login
□ MongoDB Atlas credentials
□ Gmail app password
□ 2-3 hours available time
□ Backup storage space (local machine)

═══════════════════════════════════════════════════════════════════════
  DNS MIGRATION STRATEGY
═══════════════════════════════════════════════════════════════════════

T-48h:  Lower DNS TTL to 300 seconds
T-24h:  Backup & prepare new server  
T-2h:   Deploy & test new server
T-1h:   Setup SSL certificates
T-0:    Update DNS records
T+5m:   Start monitoring
T+48h:  Decommission old server

═══════════════════════════════════════════════════════════════════════
  IMPORTANT FILES & LOCATIONS
═══════════════════════════════════════════════════════════════════════

Project:        /var/www/bhumi-interior/
Backups:        /var/backups/bhumi-interior/
Uploads:        /var/www/bhumi-interior/backend/uploads/
Logs:           /var/www/bhumi-interior/backend/logs/
Config:         /var/www/bhumi-interior/.env.production
Frontend Build: /var/www/bhumi-interior/frontend/dist/

═══════════════════════════════════════════════════════════════════════
  ENVIRONMENT CONFIGURATION
═══════════════════════════════════════════════════════════════════════

MUST UPDATE ON NEW SERVER:
• JWT_SECRET       (generate new: openssl rand -hex 32)
• DOMAIN           (if changed)
• MONGO_URI        (MongoDB Atlas connection string)
• EMAIL_USER       (Gmail address)
• EMAIL_PASS       (Gmail app password)
• SSL_EMAIL        (For Let's Encrypt)

═══════════════════════════════════════════════════════════════════════
  COST ESTIMATE (MONTHLY)
═══════════════════════════════════════════════════════════════════════

VPS/Server:        $12-30
Domain:            $1-2/mo ($10-15/year)
MongoDB Atlas:     $0 (free tier)
SSL Certificate:   $0 (Let's Encrypt)
────────────────────────────
TOTAL:             $15-35/month

═══════════════════════════════════════════════════════════════════════
  SUPPORT CONTACTS
═══════════════════════════════════════════════════════════════════════

Email:  bhumiinteriorsolution@gmail.com
Phone:  +91 92281 04285
GitHub: github.com/kenilGamer/Bhumi-interior-solution

═══════════════════════════════════════════════════════════════════════
  MIGRATION SUCCESS CRITERIA
═══════════════════════════════════════════════════════════════════════

✓ All containers running (docker ps)
✓ No errors in logs
✓ Frontend accessible
✓ Backend API responding
✓ Database connected
✓ User login works
✓ File uploads work
✓ HTTPS enabled
✓ All features functional

═══════════════════════════════════════════════════════════════════════
  EMERGENCY ROLLBACK
═══════════════════════════════════════════════════════════════════════

If migration fails, immediately:

1. Update DNS back to OLD_SERVER_IP:
   A @ OLD_SERVER_IP
   A www OLD_SERVER_IP
   A api OLD_SERVER_IP

2. Wait 5-10 minutes for DNS propagation

3. Old server resumes service

4. Investigate new server issues offline

═══════════════════════════════════════════════════════════════════════
  POST-MIGRATION TASKS
═══════════════════════════════════════════════════════════════════════

□ Setup automated backups (./scripts/setup-cron-backup.sh)
□ Install monitoring tools (htop, etc.)
□ Configure fail2ban
□ Test all features
□ Monitor logs for 24-48 hours
□ After 48h: Decommission old server
□ Increase DNS TTL back to 3600

═══════════════════════════════════════════════════════════════════════

Print this card and keep it handy during migration!

For detailed instructions, see:
  → README-MIGRATION.md (overview)
  → MIGRATION-QUICK-START.md (5-step guide)
  → MIGRATION-GUIDE.md (complete guide)

Good luck! 🚀

═══════════════════════════════════════════════════════════════════════

